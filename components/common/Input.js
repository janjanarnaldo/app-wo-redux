'use strict';

const _Input = ({ value, placeholder, id, type, className, _ref: ref, onChange }) => {
  return React.createElement('input', { value, placeholder, id, type, className, ref, onChange });
}
